import {chromium} from 'playwright';import {mkdir,writeFile} from 'node:fs/promises';
const browser=await chromium.launch({headless:true,channel:'chrome'});const page=await browser.newPage();await page.goto('http://localhost:5173');await mkdir('public/editorial',{recursive:true});for(const id of ['boneka','keris','tarot','jenglot']){const image=await page.evaluate(async id=>{const {renderPortrait}=await import('/src/portraitRenderer.js');return renderPortrait(id);},id);await writeFile('public/editorial/'+id+'.png',Buffer.from(image.split(',')[1],'base64'));console.log('Rendered',id);}await browser.close();
