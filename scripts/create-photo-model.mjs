import {chromium} from 'playwright';import {writeFile} from 'node:fs/promises';
const browser=await chromium.launch({channel:'chrome',headless:true});try{const page=await browser.newPage();await page.goto('http://127.0.0.1:5189');const result=await page.evaluate(async()=>{const {createPhotoModel}=await import('/src/photoModelAuthor.js');return createPhotoModel();});await writeFile('public/props/foto.glb',Buffer.from(result.glb));await writeFile('public/editorial/foto.png',Buffer.from(result.thumbnail.split(',')[1],'base64'));console.log('Created foto.glb and thumbnail');}finally{await browser.close();}
